apiversion:v1
metadata:
  labels:
  app: jenkins-agent
spec:
   securityContext:
      runAsUser: 1000
   containers: 
   -name: tools
    image: ansible_main_ansible
    resources:
      limits:
        memory: 800Mi
      requests: 
        memory: 50Mi
 
