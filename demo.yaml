- hosts: localhost
  gather_facts: 'false'
  tasks:
   - name: Create a k8s namespace
     k8s:
      kubeconfig: '~/config'
      name: testing1
      api_version: v1
      kind: Namespace
      state: present
      state: present
